
    {
        "private": true,
        "scripts": {
            "setup": "npm install && composer install && cd frontend && npm install ",
            "client": "npm run dev --prefix frontend",
            "dev": "concurrently \"php artisan serve\" \"npm run client \" ",
            "build": "npm run build --prefix frontend",
            "lint": "npm run lint --prefix frontend",
            "preview": "concurrently \"php artisan serve\" \"npm run preview --prefix frontend\" "
    
        },
       
    "devDependencies": {
        "axios": "^0.21",
        "laravel-mix": "^6.0.6",
        "concurrently": "^8.2.2",
        "lodash": "^4.17.19",
        "postcss": "^8.1.14"
    }

}
